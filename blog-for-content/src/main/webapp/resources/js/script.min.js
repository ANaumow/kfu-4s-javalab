function expandTextarea(e) {
    let t = e;
    t.style.overflow = "hidden", t.style.height = 0, t.style.height = Math.max(t.scrollHeight, 40) + "px"
}

$(document).ready(function () {
    AOS.init({disable: "mobile"})
});